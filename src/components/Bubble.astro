---
import { twJoin } from 'tailwind-merge';

type Props = {
  speaker: string;
  feLevel: string;
};

const { speaker, feLevel } = Astro.props;
---

<dl class="flex gap-[1.1875rem] md:gap-7">
  <dt class="w-9 min-w-[2.25rem] text-center md:w-12 md:min-w-[3rem]">
    <img
      src={speaker === '小中' || speaker === 'Konaka'
        ? '/assets/images/konaka-fe' + feLevel + '.png'
        : speaker === '隅木' || speaker === 'Sumiki'
        ? '/assets/images/sumiki-fe' + feLevel + '.png'
        : '/assets/images/daiin-fe' + feLevel + '.png'}
      alt=""
      decoding="async"
      width="48"
      height="48"
    />
    <p class="text-[0.6875rem]/relaxed font-bold">{speaker}</p>
  </dt>
  <dd
    class={twJoin(
      'relative grow rounded-lg bg-white p-2 md:p-4',
      'before:absolute before:left-[-0.9375rem] before:top-4 before:block before:border-y-[6px] before:border-r-[15px] before:border-y-transparent before:border-r-white md:before:-left-5 md:before:border-y-8 md:before:border-r-[20px]'
    )}
  >
    <slot />
  </dd>
</dl>
